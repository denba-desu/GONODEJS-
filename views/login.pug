doctype html
html(lang='en')
    head
        meta(charset='utf-8')
        title Log In
        // Google Fonts
        link(href='https://fonts.googleapis.com/css?family=Roboto+Slab:400,100,300,700|Lato:400,100,300,700,900', rel='stylesheet', type='text/css')
        link(rel='stylesheet', href='css1/animate.css')
        // Custom Stylesheet
        link(rel='stylesheet', href='css1/style.css')
        script(src='https://ajax.googleapis.com/ajax/libs/jquery/2.1.4/jquery.min.js')
    style.
        /* Full-width input fields */
        input[type=text], input[type=password] {
        width: 50%;
        padding: 12px 20px;
        margin: 8px 0;
        display: inline-block;
        border: 1px solid #ccc;
        box-sizing: border-box;
        }
        /* Set a style for all buttons */
        button {
        background-color: #665851;
        color: white;
        padding: 14px 20px;
        margin: 8px 0;
        border: none;
        cursor: pointer;
        width: 25%;
        float: center;
        }
        /* Extra styles for the cancel button */
        .cancelbtn {
        padding: 14px 20px;
        background-color: #f44336;
        }
        /* Float cancel and signup buttons and add an equal width */
        .cancelbtn,.signupbtn {float:center;width:25%}
        /* Add padding to container elements */
        .container1 {
        padding: 16px;
        }
        /* The Modal (background) */
        .modal {
        display: none; /* Hidden by default */
        position: fixed; /* Stay in place */
        z-index: 1; /* Sit on top */
        left: 0;
        top: 0;
        width: 100%; /* Full width */
        height: 100%; /* Full height */
        overflow: auto; /* Enable scroll if needed */
        background-color: rgb(0,0,0); /* Fallback color */
        background-color: rgba(0,0,0,0.4); /* Black w/ opacity */
        padding-top: 60px;
        }
        /* Modal Content/Box */
        .modal-content {
        background-color: #fefefe;
        margin: 5% auto 15% auto; /* 5% from the top, 15% from the bottom and centered */
        border: 1px solid #888;
        width: 80%; /* Could be more or less, depending on screen size */
        }
        /* The Close Button (x) */
        .close {
        position: absolute;
        right: 35px;
        top: 15px;
        color: #000;
        font-size: 40px;
        font-weight: bold;
        }
        .close:hover,
        .close:focus {
        color: red;
        cursor: pointer;
        }
        /* Clear floats */
        .clearfix::after {
        content: "";
        clear: both;
        display: table;
        }
        /* Change styles for cancel button and signup button on extra small screens */
        @media screen and (max-width: 300px) {
        .cancelbtn, .signupbtn {
        width: 100%;
        }
        }
    body
        .container
            .top
                h1#title.hidden
                    span#logo
                        | iTuToR 
                        span LogIn
            .login-box.animated.fadeInUp
                .box-header
                    h2 Service Provider!
                form(action='/', method='POST')
                    label(for='username') Username
                    br
                    input#username(type='text', name='username')
                    br
                    label(for='password') Password
                    br
                    input#password(type='password', name='password')
                    br
                    button(type='submit') Log in!
                    br
                p.small
                    | Dont Have an Account?
                    br
                button(onclick="document.getElementById('id01').style.display='block'", style='width:auto;', type='submit') Sign Up!
        #id01.modal
            span.close(onclick="document.getElementById('id01').style.display='none'", title='Close Modal') ×
            form.modal-content.animate(action='php1/registration.php', method='POST')
                .container1
                    label
                        b Name
                    br
                    input(type='text', placeholder='Enter Name', name='name', required='')
                    br
                    label
                        b Email
                    br
                    input(type='text', placeholder='Enter Email', name='email', required='')
                    br
                    label
                        b Password
                    br
                    input(type='password', placeholder='Enter Password', name='psw', required='')
                    br
                    label
                        b Repeat Password
                    br
                    input(type='password', placeholder='Repeat Password', name='psw-repeat', required='')
                    br
                    label
                        b Skills
                    br
                    input(type='text', placeholder='Enter Skills separated by (,) followed by a whitespace', name='skills', required='')
                    br
                    label
                        b Contact #
                    br
                    input(type='text', placeholder='+63xxxxxxxx', name='cpNumber', required='')
                    br
                    label
                        b Address
                    br
                    input(type='text', placeholder='Enter Address', name='addrss', required='')
                    br
                    input(type='radio', name='gender', value='M')
                    |  Male
                    br
                    input(type='radio', name='gender', value='F')
                    |  Female
                    br
                    br
                    input(type='checkbox', checked='checked')
                    | By creating an account you agree to our 
                    a(href='#') Terms & Privacy
                    | .
                    .clearfix
                        button.cancelbtn(type='button', onclick="document.getElementById('id01').style.display='none'") Cancel
                        button.signupbtn(type='submit') Next
    script.
        // Get the modal
        var modal = document.getElementById('id01');
        // When the user clicks anywhere outside of the modal, close it
        window.onclick = function(event) {
        if (event.target == modal) {
        modal.style.display = "none";
        }
        }
    script.
        $(document).ready(function () {
        $('#logo').addClass('animated fadeInDown');
        $("input:text:visible:first").focus();
        });
        $('#username').focus(function() {
        $('label[for="username"]').addClass('selected');
        });
        $('#username').blur(function() {
        $('label[for="username"]').removeClass('selected');
        });
        $('#password').focus(function() {
        $('label[for="password"]').addClass('selected');
        });
        $('#password').blur(function() {
        $('label[for="password"]').removeClass('selected');
        });
